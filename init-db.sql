-- Inicializar banco de dados PostgreSQL
-- Este arquivo é executado automaticamente na primeira inicialização

-- Verificar se o banco yuni_db já existe, se não, criar
-- (O PostgreSQL já cria via POSTGRES_DB, mas isso garante)

SELECT 'Database already exists' as message;

